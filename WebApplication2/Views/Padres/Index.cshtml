@using System.Configuration
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_plantillaPadres.cshtml";
}

<h3>Hola, @ViewData["Nombre"].<br />
   Esta es la información de tus hijos registrados, si hay algun error contacta al soporte para una corrección de información.<br />
    </h3>



@{Html.RenderAction("Hijos","Padres", new { id = ViewData["id"] });}

<p>
    <h2>
        Su saldo actual es: $@ViewData["saldo"].<br />
        Si quiere agregar mas saldo, por favor ingrese una cantidad y continue con el proceso de pago.<br />
    </h2>
</p>
Pago por paypal

<form action="@ConfigurationManager.AppSettings["urlSubmitPayment"]">
    <input type="hidden" name="cmd" value="_xclick" />
    <input type="hidden" name="upload" value="1" />
    <input type="hidden" name="return" value="@ConfigurationManager.AppSettings["urlReturn"]" />
    <input type="hidden" name="business" value="@ConfigurationManager.AppSettings["accountBusiness"]" />
    <input type="hidden" name="item_name" value="Agregar saldo a KidsCoin" />
    <input type="hidden" name="item_number" value="@ViewData["ID"]" />
    <input type="number" name="amount" value="" placeholder="Agrege cantidad" />

    <br />
    <div class="col-md-12">
        <input type="submit" value="Procesar solicitud" class="btn btn-primary col-md-12" />
    </div>
</form>

